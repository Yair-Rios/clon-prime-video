import React from 'react';
import Menu from '../Components/Menu';

const Video = () => {
    return (
        <div>
            video
            <Menu/>
        </div>
    );
};

export default Video;